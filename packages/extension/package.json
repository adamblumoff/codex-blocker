{
  "name": "@codex-blocker/extension",
  "version": "0.0.10-alpha.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/service-worker.ts src/content-script.ts src/popup.ts src/options.ts --format iife --out-dir dist --clean && node scripts/generate-icons.mjs && cp src/popup.html src/popup.css src/options.html src/options.css src/assets/icon-mark.svg manifest.json dist/",
    "dev": "pnpm build --watch",
    "typecheck": "tsc --noEmit",
    "zip": "cd dist && zip -r ../codex-blocker.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "sharp": "^0.34.5",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
